BREAKING: Individual begin flags are now variants of a `BeginFlag` enum, rather than constants.
BREAKING: `BeginFlag` has a different API - it's now an `EnumSet` from `enumset` in the new wrapper struct `BeginFlags`.
